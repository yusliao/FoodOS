# FoodOS 运营方与饭店租户改造实施清单

日期：2026-09-15。状态：阶段 0—3 已完成，阶段 4 待实施。

本文件用于按顺序实施、验收和交接，不代表所列功能已经完成。后续任务先读取本文件，找到第一个未完成阶段；完成实现及验证后才勾选对应条目，并更新末尾执行记录。

## 1. 已确认的业务方向

- 系统购买者、拥有者和经营主体是一家运营方，一套部署服务该运营方及其合作饭店。
- 租户代表饭店客户，客户通过 Shop 向运营方订货并接受履约服务。
- admin 是运营方员工工作台，承接采购、库存、仓储、配送、客户及经营管理。
- dashboard 收敛为饭店客户门户，承接订货、订单、交付进度、售后与账号管理。
- 保留两个前端及现有模块化单体后端，不增加第三个前端，不拆微服务。
- 商品、供应商、采购、仓库、库存和履约作业属于运营方。客户不能因为成为租户管理员就获得内部经营权限。
- 订单、售后和交付保留一份权威业务记录；通过明确的客户归属与参与者权限支持双方操作，不复制两套单据。

本次用户确认优先于历史设计中“租户拥有完整供应链”“冲突时以脚手架为准”等不一致表述。保留源生成 Mediator、Contracts、EF Core、React、USD 默认币种以及当前语言配置等工程约束。

## 2. 当前问题与验收目标

当前源码把客户、门店、订单、仓库和库存放在相同的技术租户边界内；tenant 在业务能力上更接近独立运营企业。Basic 默认具有多个内部模块的查看权限；dashboard 路由主要只判断登录；Shop 缺少用户与门店的授权范围校验。

目标验收场景：饭店 A、B 以不同客户租户登录，同享运营方发布的可售商品，分别获取适用价格并下单；运营方统一采购、备货、配送；A、B 无法读取或修改彼此的订单、购物车、售后、价格和文件；员工按岗位操作，司机按任务归属操作。

检查结论来自静态源码，不能视为已验证当前 Docker 数据库或部署镜像。阶段 0 必须核对实施时的实际状态。

## 3. 实施原则与范围

- 顺序推进阶段 0—8，每个阶段先形成可验证的最小切片，再扩大范围。
- 每次先检查工作区及同类实现，保留其他任务修改，不自动提交、推送或新建分支。
- 使用现有模块；跨模块只能通过 Contracts。共享框架仅修改完成本次隔离目标所必需的部分，不做无关重构。
- 权限包含身份、动作权限、数据范围三部分。隐藏菜单、客户端 header、用户传入的 StoreId/TenantId 均不构成授权。
- 缺少身份或范围时默认拒绝；后台任务不得在缺少范围时自动当作超级管理员执行。
- 不在各 Handler 散布 IgnoreQueryFilters、任意切换租户或通用“跳过权限”开关。
- 不把所有业务实体标记为全局实体来绕过现有租户过滤。
- 保持库存账、幂等、并发、软删除、审计及状态机已有有效语义。
- 本次包含现有业务适配，不擅自扩展完整应收账款、税务、支付或新的结算模块。
- 生成迁移、检查脚本、测试方案属于实施准备；执行存量数据库迁移、批量回填、文件删除和生产发布仍按仓库规则取得对应明确授权。

## 4. 数据与身份设计基线

### 4.1 数据归属

- 运营方拥有：商品主档、供应商、采购、批次、库存流水、库位、仓储作业、车辆、线路和配送任务。
- 客户拥有访问范围：客户账号、授权门店、购物车、客户设置及客户侧文件。
- 交易记录明确标记客户租户及门店归属；运营侧读取需要员工身份和业务权限，客户侧读取必须限制客户租户及授权门店。
- 客户可见商品是运营商品的受控投影；不得返回成本价、供应商底价、内部备注、全仓库存明细等内部字段。
- 协议价由服务端根据已验证的客户关系解析，不能通过修改 CustomerOrgId 查询他人价格。
- 客户账号和运营员工仍复用 Identity，但两者身份及默认角色集合必须明确区分。

### 4.2 需要在阶段 0 固化的设计

- 现有技术 TenantId 的具体保留和迁移方式：区分身份租户与业务所有者，不能让同一字段在不同调用中暗中切换含义。
- 运营方单一实例的存储方式：不为了未来多运营方预建平台；也不把 root 超级管理员权限当普通员工权限。
- 饭店租户与 CustomerOrg、Store 的映射：默认面向单店客户，复用已有门店实体；未明确授权时客户只能访问绑定门店，不默认拥有同 tenant 下全部门店。
- 连锁多门店是否需要客户管理员分配成员权限。该决定只影响客户范围扩展，不阻止单店闭环实施。
- 既有“签收”是司机电子交付凭证还是客户收货确认；在明确规则前不改变完成订单的状态转换。
- 软件订阅 Billing 与采购货款结算分开；现有 Billing 不通过重命名充当交易账单。

设计取舍及源码依据直接记录在本文件的执行记录或链接的专题设计中。涉及实际数据归属的歧义必须单独列出，不能由脚本猜测。

## 5. 按阶段实施清单

### 阶段 0：基线核对与设计冻结（已完成）

- [x] 0.1 核对当前分支、未提交修改、Docker Compose 服务及镜像来源；只读确认运行状态。
- [x] 0.2 列出 admin/dashboard 页面、接口、权限、实际数据归属及现有测试，形成迁移映射。
- [x] 0.3 检查 Identity 默认角色、权限同步、租户初始化与演示种子，标出会重新授予宽权限的路径。
- [x] 0.4 固化第 4 节的数据模型与租户存储方案；列出实体、字段、唯一索引、外键、查询范围和 Contracts 变化。
- [x] 0.5 明确单店默认行为及未决项，明确现有交易结算与签收的本轮范围。
- [x] 0.6 在业务规范及设计入口记录新业务定义，避免后续任务按旧租户含义回退。

验收：每类数据的所有者、客户访问范围、运营访问方式均能明确说明；迁移方案不依赖绕过过滤器或复制订单。

### 阶段 1：身份、权限与访问范围基础（已完成）

依赖：阶段 0。

- [x] 1.1 建立运营员工与客户用户的显式身份判定，登录、刷新令牌及恢复会话均执行一致规则。
- [x] 1.2 补齐客户租户—客户组织—门店—用户授权关联与必要约束。
- [x] 1.3 在既有模块及 Contracts 中提供可复用的身份与业务范围解析；HTTP、后台任务、事件消费者均有明确入口。
- [x] 1.4 区分运营岗位和客户岗位权限，移除客户默认角色对内部采购、仓储、物流、审计等内容的默认授权。
- [x] 1.5 调整默认 Admin/Basic 与权限同步策略，禁止客户管理员给自己或他人授予运营身份、内部岗位或 root 权限。
- [x] 1.6 校验运营角色管理、客户成员管理、用户创建、自助注册、组角色继承及模拟登录路径。
- [x] 1.7 完成迁移文件及受影响测试；不得仅修改 seed 并认为存量角色已修复。

验收：客户伪造 app header 或目标租户、申请内部角色、修改用户关联均被拒绝；员工通过明确岗位权限工作；root 保留系统恢复用途。

### 阶段 2：运营方数据归属及迁移准备（已完成）

依赖：阶段 1。

- [x] 2.1 按设计调整 Catalog、Inventory、Procurement、Warehouse、Logistics 的持久化范围与模块 Contracts。
- [x] 2.2 调整 Ordering 客户关联和交易归属，明确购物车、订单、售后与运营库存的引用关系。
- [x] 2.3 检查索引、外键、同号数据冲突、软删除过滤、幂等键作用域和库存流水关联。
- [x] 2.4 检查订单号、采购单号、批次号、库存唯一键在新归属下的冲突规则。
- [x] 2.5 提供迁移前只读盘点与校验方案；按来源明确保留、映射、合并和人工处理的规则。
- [x] 2.6 准备新增结构、回填、校验、切换和旧结构清理顺序；列明代码与数据库兼容窗口。
- [x] 2.7 调整后台任务、Outbox/Inbox、事件负载、缓存键、搜索和文件访问的客户/运营范围。
- [x] 2.8 更新测试数据及演示种子为“单运营方 + 两个饭店客户 + 多岗位员工”。

验收：饭店无需拥有内部仓库即可引用运营方可供货能力；A/B 数据不会因共享库存被合并；迁移脚本可在隔离测试库复现且重复执行有明确行为。

### 阶段 3：后端 Shop 最小闭环（已完成）

依赖：阶段 2。

- [x] 3.1 提供客户专用商品查询及 DTO，限制发布状态、可售范围、适用价格和库存展示粒度。
- [x] 3.2 门店查询仅返回当前用户授权门店，详情接口执行同样范围限制。
- [x] 3.3 购物车读写、下单、订单列表/详情、改单及取消均执行客户和门店范围检查。
- [x] 3.4 运营侧提供订单汇总、详情和履约入口，与客户接口共用领域规则，不复制状态机。
- [x] 3.5 打通饭店订单使用运营库存的预占、失败补偿、取消释放、并发和幂等。
- [x] 3.6 售后创建及查询检查原订单归属、门店范围和允许状态。
- [x] 3.7 完成 A/B 下单、运营汇总以及双向越权失败的集成测试。2026-09-16 已补齐 B 真实下单及双向详情、筛选、购物车、下单、改单、取消、售后越权负例。

验收：A/B 使用同一运营商品及库存下单；运营方统一看到两方订单；A 不能通过 ID、筛选参数或请求体访问/操作 B 的对象；并发下库存与单据一致。

### 阶段 4：运营履约与辅助通道隔离

依赖：阶段 3。

- [x] 4.1 验证采购与质检分权，客户无法读取供应商及采购内部数据。
  - 2026-09-16：补齐采购员质检通过/拒收 403、质检员供应商创建及采购建单/发送/预约 403；真实饭店管理员读取供应商和采购单列表/详情、操作现有采购写端点均为 403，拒绝后采购状态、质检记录及 ATP 未改变。隔离 PostgreSQL 集成测试筛选 `FullyQualifiedName~Tests.Procurement`：4 通过、0 失败、0 跳过。现有端点权限满足本项，无需修改生产逻辑。
  - 按用户最新要求顺序续接：下一项为 4.2，不再先推进 4.6。4.2 尚未完成；当前 PickTask.PickerUserId 在确认时记录操作人，不是预先分配字段，不能据此宣称已有个人任务隔离。
- [ ] 4.2 验证上架、波次、拣货、装托、发运权限与必要的任务分配范围。
- [x] 4.3 司机查询和签收操作均验证任务归属，不只限制 mine 列表。
- [ ] 4.4 客户交付查询仅返回本门店相关内容，混合配送任务不能泄漏其他客户订单和地址。
- [ ] 4.5 审计与运营报表仅按运营岗位开放；客户必要的订单轨迹使用受控投影。
- [ ] 4.6 明确工单参与者范围，通知、SSE、SignalR、聊天、附件、下载链接和导出应用同样隔离。
- [ ] 4.7 禁止通过全局用户搜索、角色查询及模拟登录扩大客户可见范围。
- [ ] 4.8 对现有对账能力进行双方访问适配；新增货款结算功能单独列范围，不复用软件订阅账单冒充。

验收：隐藏在列表之外的数据不能通过详情、下载、实时推送或作业写入接口获得；既有库存账及履约状态回归通过。

### 阶段 5：admin 运营工作台迁移

依赖：阶段 3；履约页面以阶段 4 对应接口验收为前提。

- [ ] 5.1 接入运营员工登录与岗位权限，更新导航及默认首页。
- [ ] 5.2 迁入商品、定价、合作客户和门店管理。
- [ ] 5.3 接入运营订单中心、售后处理及已有对账入口。
- [ ] 5.4 迁入采购、质检、库存、上架、波次、拣货和配送工作台。
- [ ] 5.5 接入经营报表、审计及运营员工岗位管理；系统恢复/配置入口保持单独权限。
- [ ] 5.6 每个页面配置路由权限、按钮权限和按权限启用的请求；完善 403/404、空态和加载态。
- [ ] 5.7 使用 admin 现有组件、API client、查询键和本地化方式，避免跨应用引用页面或复制长期并存的业务实现。
- [ ] 5.8 执行类型检查及关键岗位端到端测试。

验收：采购员、质检员、仓管、司机、财务按岗位完成各自任务；没有权限时不能通过直输 URL 绕过页面入口；实际 API 仍独立授权。

### 阶段 6：dashboard 饭店门户收敛

依赖：阶段 3；内部页面退场以阶段 5 对应功能可用为前提。

- [ ] 6.1 首页以订货和本店订单为中心，移除软件经营平台的定位文案。
- [ ] 6.2 接入客户专用商品、协议价、购物车、订单和售后接口。
- [ ] 6.3 门店选择器仅展示授权范围；单店用户默认进入所属门店。
- [ ] 6.4 保留客户账号、安全、通知和必要的成员管理，客户管理员权限限制在自己的客户范围。
- [ ] 6.5 退役内部采购、仓储、物流管理、全量审计、软件订阅等菜单与路由；旧 URL 显示明确的无权限或退役结果。
- [ ] 6.6 调整演示登录账号、运行配置及登录/刷新/模拟登录边界，不再宣传无法登录的 root 账号。
- [ ] 6.7 退出、换用户、换租户和换门店清理相应查询缓存及本地选择；缓存键包含必要的身份/范围。
- [ ] 6.8 执行类型检查、客户 A/B 场景及移动端主要交互测试。

验收：饭店用户只接触授权客户业务；隐藏入口、直接 URL、刷新页面和切换账号后的行为一致。

### 阶段 7：整体回归与迁移演练

依赖：阶段 4—6。

- [ ] 7.1 使用 PostgreSQL 集成测试覆盖真实过滤、关联、权限继承与并发，不仅依靠 Mock。
- [ ] 7.2 覆盖 A 读写 B 的门店、价格、购物车、订单、售后、附件和通知均失败。
- [ ] 7.3 覆盖授权撤销、租户停用、旧令牌、缓存失效及后台作业范围。
- [ ] 7.4 回归采购质检分权、预占与释放、上架去重、拣货、发运、签收重试及对账。
- [ ] 7.5 回归 admin/dashboard 类型检查、构建和真实端到端闭环；标明哪些测试使用接口 Mock。
- [ ] 7.6 执行模块边界/架构测试，检查新增跨模块引用与迁移一致性。
- [ ] 7.7 在隔离环境演练迁移，比较迁移前后订单金额、单据数量、库存余额、流水及关键关联。
- [ ] 7.8 制定备份恢复与失败处理步骤，说明迁移后产生新业务数据时不能简单倒退数据库。

验收：所有关键隔离负例及业务闭环通过；迁移差异有解释；无未处理的高风险访问路径。

### 阶段 8：发布、清理与运维交接

依赖：阶段 7；实际发布及存量数据变更需对应执行授权。

- [ ] 8.1 提供具体发布清单：备份、暂停写入/后台任务、迁移、校验、服务切换、冒烟和恢复作业。
- [ ] 8.2 核对 Compose 两端配置、登录边界、种子密码和演示开关，生产不运行演示种子重置账号。
- [ ] 8.3 按授权执行发布并记录版本、迁移结果和业务核对结果。
- [ ] 8.4 确认无使用方后清理旧页面、失效 API、旧字段及兼容代码；文件删除按项目规则取得授权。
- [ ] 8.5 更新 README、业务规范、设计、API说明、岗位账号和故障排查指引。
- [ ] 8.6 记录必要的运行指标和排障线索：授权失败、订单处理失败、库存不一致、后台重试和迁移异常。

验收：两端入口职责明确；恢复方案可操作；没有长期无人负责的旧权限和双份业务实现。

## 6. 每个实施切片的完成定义

- [ ] 已阅读本清单、相关规范与对应模块现状。
- [ ] 变更覆盖所有实际入口，包括详情/列表、命令、后台任务和实时通知中相关部分。
- [ ] 必要的正向测试与越权负向测试通过；记录命令、环境、结果及未覆盖项。
- [ ] 存量数据与部署兼容影响已说明；迁移执行状态与代码完成状态分开记录。
- [ ] 本清单的条目与执行记录已更新，没有用“已写代码”替代“验收通过”。

## 7. 执行记录与续接入口

### 2026-09-15：建立实施基线

- 已完成：确认系统购买者为运营方、租户为饭店客户，确定 admin 承接运营、dashboard 收敛为客户门户；形成本文阶段顺序与验收清单。
- 未开始：业务代码、数据库结构、存量数据及部署改造。上文阶段条目全部保持未勾选。
- 当前源码另有 dashboard 工单及本地化未提交修改，后续实施需重新核对，不能覆盖。
- 下一步：阶段 0.1—0.6，形成实体及接口迁移映射并冻结租户存储设计，再实施阶段 1。
- 后续记录格式：日期、阶段/条目编号、变更文件或设计链接、验证结果、数据变更状态、未决事项、下一项。

### 2026-09-15：完成阶段 0

- 完成 0.1—0.6：核对工作区、前后端权限与数据边界，建立 [目标架构](FoodOS-运营方与饭店租户目标架构.md)。
- 决定：root 为运营身份域、非 root 为饭店身份域；运营实体为单运营方共享数据；客户与交易显式保存 CustomerTenantId 和 StoreId；不通过请求内切换租户或复制订单解决共享。
- 页面映射：运营页面迁入 admin，dashboard 保留饭店 Shop、交付、售后和客户账号能力。
- 数据状态：仅文档设计完成，未生成或应用数据库迁移。
- 下一步：阶段 1，先收紧登录应用边界和默认权限，再建立客户组织、门店、用户授权结构。

### 2026-09-15：完成阶段 1

- 身份边界：JWT 增加 `business_actor`；签发、刷新、两端会话恢复均执行 root/operator 与非 root/customer 的双向应用边界。
- 权限边界：权限目录增加客户可授予标记；非 root Admin/Basic 只同步客户权限；同步器会清理存量客户角色中的运营/root 权限，角色增加 `Audience` 并按原 `TenantId` 回填。

### 2026-09-15：完成阶段 2

- 数据所有权：新增显式 `IOperatorOwnedEntity`，Catalog、Inventory、Procurement、Warehouse、Logistics 及 Ordering 权威记录统一使用 root 运营所有权过滤；保留旧 `TenantId` 兼容列，不在 Handler 中散布过滤器绕过。
- 客户归属：CustomerOrg、Store、CustomerUserStoreAccess、Cart、SalesOrder、AfterSalesTicket 使用显式 CustomerTenantId/StoreId；交易 DTO 和创建链路同步传递归属。
- 结构迁移：生成六个模块迁移；修复 Catalog 软删除唯一索引和 Warehouse 波次部分唯一索引的过滤条件。六个 DbContext 的 `has-pending-model-changes` 均为无差异。
- 作业与事件：临期、配送提醒、对账提醒固定在 root 运营域单次执行，避免按所有饭店租户重复扫描；Outbox/Inbox 继续保留事件发起身份域，运营作业事件使用 root，客户事件保留客户身份租户。
- 演示数据：root 创建采购、质检、仓储、配送、财务等运营岗位；Acme/Globex 仅作为饭店身份租户，并显式映射 CustomerOrg、Store 和用户门店授权。
- 迁移准备：新增 [运营数据迁移运行手册](FoodOS-运营数据迁移运行手册.md)、[只读盘点 SQL](sql/FoodOS-operator-migration-preflight.sql) 和默认回滚的 [归属回填模板](sql/FoodOS-operator-migration-backfill-template.sql)。未对当前数据库执行迁移或回填。
- 验证：解决方案构建 0 警告/0 错误；Architecture 52、Ordering 22、Inventory 36、Logistics 7 项测试通过；PostgreSQL OrderingShop 3 项集成测试通过并实际创建最新迁移结构。
- 下一步：阶段 3.1，从客户专用商品投影、适用价格及可售范围开始，再依次接入门店、购物车、下单与运营订单入口。
- 数据范围：Ordering 增加 `CustomerTenantId`、用户门店授权表、集中 `ICustomerAccessScopeResolver`、客户管理员授权接口，以及单店注册用户自动绑定入口。
- 迁移：已生成 `BusinessRoleAudience` 与 `CustomerTenantStoreAccess`；仅生成并在测试容器应用，未应用到开发或生产数据库。
- 验证：解决方案构建通过；Identity 318、Ordering 22、Architecture 51 项通过；PostgreSQL 登录/越权集成测试 9 项及非 root 反向登录断言通过；admin/dashboard 构建通过。
- 下一步：阶段 2，将运营主数据和 Ordering 交易数据切换为单运营方共享所有权，并补齐迁移盘点与演示种子。

### 2026-09-15：完成阶段 3

- 客户接口：新增 `/api/v1/shop` 商品、授权门店、购物车、订单和售后投影；商品仅返回启用商品、客户适用价及受控字段，不暴露成本、供应商和全仓库存明细。
- 范围校验：所有客户读写先解析 `CustomerTenantId + CustomerOrgId + StoreIds`；越权门店、订单和售后创建统一按不存在处理，旧 Catalog 全量商品及 Ordering 运营接口不再授予客户角色。
- 单据复用：客户 Shop 命令只负责访问范围校验，随后复用既有购物车、订单和售后命令；运营订单中心使用同一 SalesOrder 权威记录和状态机。
- 性能与可靠性：商品有效性和客户价格改为批量查询，避免订单行 N+1；下单、改单、取消和售后 Shop 入口接入既有幂等过滤，继续复用库存预占、释放及失败补偿。
- 验证：Architecture 52、Catalog 95、Ordering 22 项测试通过；PostgreSQL 集成测试 5 项通过。后续复核纠正：客户隔离场景实际覆盖 A 下单、B 越权、授权门店隔离、售后越权和运营查看，并非 A/B 均实际下单及双向越权；完整双向验收列回 3.7。另有并发预占及失败补偿回归。
- 数据状态：仅测试容器应用迁移；未对开发或生产数据库执行迁移或回填。
- 下一步：阶段 4.1，逐项检查采购、仓储、物流、交付、审计、工单/通知/聊天/文件及对账的操作与数据范围。

### 2026-09-15：阶段 4 第一切片（进行中）

- 司机范围：新增 `Logistics.Shipments.ViewAssigned`，司机角色不再持有全量发运查看权限；`mine` 列表、分配任务详情和 POD 签收均按当前用户对应 Driver 与 Shipment.DriverId 校验，root Admin 仅保留恢复旁路。
- 客户交付：新增 `/api/v1/shop/deliveries` 受控投影；Ordering 先解析客户授权门店，再由 Logistics 仅返回这些门店的站点状态、相关订单号和签收时间，不返回路线内其他站点、地址、车辆、司机、批次或内部退货信息。
- 权限负例：PostgreSQL 场景已验证饭店用户访问运营门店全集、供应商、仓储波次、全量配送、审计和运营对账均返回 403；客户交付空态正常返回。
- 回归：Architecture 52、Logistics 7 项通过；饭店 A/B 隔离及完整发运签收 2 项 PostgreSQL 集成测试通过。
- 未完成：混合路线交付裁剪实测、非分配司机详情/签收负例，以及工单参与者、通知/实时通道、附件和用户搜索的联合审查。因此阶段 4 条目暂不勾选。
- 下一步：先补 4.3—4.4 的混合路线和司机越权测试，再处理 4.6 的跨身份域工单协作模型。

### 2026-09-16：阶段 4 司机、工单与附件隔离

- 4.3：使用两名真实运营司机账号验证任务列表、详情与 POD 签收归属；未分配司机详情/签收返回 404，司机访问全量详情返回 403。管理员签收例外显式要求 root 身份域，不再仅凭 Admin 角色名。
- 4.4：混合配送 fixture 验证 A/B 各自只得到所属门店和订单引用，越权门店筛选返回 404；其中 B 订单引用是测试生成的 ID，并非真实 B 下单履约，完整混合配送验收仍保留未完成。
- 4.6：工单列表、详情、评论、回收站及相关客户命令统一检查报告人/受派人；空身份默认拒绝。工单附件复用参与者范围并额外检查 Tickets.View；仅允许私有上传，禁止改为公开，非参与者无法取得元数据和下载链接、上传或删除附件，工单删除后不再签发下载链接。
- 模块边界：Tickets 仅新增 Files.Contracts、Identity.Contracts 依赖；没有新增模块、跨模块 DbContext 引用或宿主注册。
- 测试修复：租户初始化等待从全文搜索 Completed 改为判断顶层状态，避免子步骤完成时提前登录；扩大回归时曾出现 1 次 401，不能把该次运行记为全通过。
- 最终验证：Architecture.Tests 52 项、Files.Tests 23 项通过；Integration.Tests 使用 `FullyQualifiedName~Tickets|FullyQualifiedName~LogisticsShipmentTests|FullyQualifiedName~CustomerShopIsolation|FullyQualifiedName~Tests.Files|FullyQualifiedName~ProductFileAccessPolicy` 筛选，最新一次 115 项通过、0 跳过（33 秒）。此前一轮 114 通过/1 失败，失败在 root 登录准备阶段，耗时约 45 分钟后返回 500；根因未确认，新容器重跑未复现，保留此异常记录。`git diff --check` 通过。
- 未完成：运营客服跨身份域协作、聊天/通知/实时通道联合检查、已有下载链接的有效期及存储访问配置验收。已签发链接不会因本次应用权限变更立即撤销；未执行存储或存量数据修改。
- 数据状态：仅隔离 PostgreSQL 测试容器使用迁移；未修改开发或生产数据库。前端尚未进入本切片。
- 下一步：补齐 3.7 双向真实下单测试，再继续阶段 4 辅助通道与岗位范围验收；不以当前局部测试替代阶段 4 总体验收。

### 2026-09-16：补齐 3.7 与聊天辅助通道隔离

- 3.7：两个饭店通过 Shop 对同一运营商品分别真实下单（3/4 单位）；双方订单列表各自独立，详情、门店筛选、购物车读写、伪造门店下单、改单、取消、售后创建均做双向 404 断言。运营订单汇总确认两单客户归属、数量和状态未被越权请求改变。混合配送 fixture 现引用两份真实订单，但仍不是完整仓配发运场景，4.4 不提前勾选。
- 4.6：Identity Contracts 增加当前身份租户的活跃用户 ID 批量查询；Chat 邀请成员、创建单聊/群聊前统一校验，未知、跨租户或停用用户不能加入。没有新增运行时模块引用、数据库迁移或宿主注册。
- 提及与附件：仅当前频道成员可产生提及通知；非成员、已移除成员不会通过通知或 SignalR 获得消息摘要。ChatChannel 附件只能私有上传，禁止切换为公开；已有外部公开链接和对象存储策略仍需单独盘点，代码不会撤销已经发出的链接。
- 实时推送：消息新增/编辑/删除/置顶、反应、成员变化、已读回执按当前频道成员的用户组发送，不再把旧频道组当授权依据。共享实时接口仅新增成员 ID 查询供输入状态推送使用；旧 JoinChannel 协议暂保留兼容，但旧连接加入的频道组不再承载上述敏感内容。集成测试验证成员在不断开连接的情况下被移除后，不再收到新消息及输入状态。
- 验证：`Integration.Tests` 筛选 `FullyQualifiedName~CustomerShopIsolation|FullyQualifiedName~Tests.Chat`，93 项通过、1 项既有跳过；跳过项是聊天幂等重放（测试环境两种缓存存储不一致，未视为通过）。Architecture.Tests 52、Identity.Tests 318、Chat.Tests 36 项通过；未跑前端或整个解决方案全量回归。
- 技术约束：复用既有 Identity Contracts 和租户过滤，按项目功能切片、模块边界、EF 查询及授权技能执行；没有跨模块直接查询身份表，没有新增框架或第三套前端。
- 数据状态：仅隔离测试容器；未对当前数据库或对象存储执行迁移、回填或权限更改。
- 下一步：继续阶段 4，落实运营客服跨身份域工单协作、通知/SSE/导出及用户搜索联合隔离，补齐岗位与完整混合配送验收，再进入 admin/dashboard 页面迁移。

### 2026-09-16：客服工单跨身份域正文/评论协作

- 4.6 部分完成：工单/评论显式保存 `CustomerTenantId`，映射原物理 `TenantId` 列；保留已有值、原单号和每客户唯一键，不复制运营工单或在请求期间切换租户。新增 `TicketAudience` 集中过滤，仅 root 获得运营查询范围；客户仍须匹配本身份域并通过报告人/受派人检查。
- 运营岗位：无工单动作权限的 root 用户返回 403；授权客服可查看各客户工单、回复、指派有效 root 用户并解决。客户不能指定处理人或执行解决；创建时带处理人额外检查 Assign 权限，防止通过 Create 绕过岗位分权。
- 客户闭环：饭店 A/B 分别提交工单，同号 `TK-1` 可在不同客户中并存；客服可回复 A，A 可回复并关闭已解决工单。双向详情、评论读写、删除和跨客户恢复负例均有集成断言；软删除过滤与客户过滤保持独立。
- 迁移：生成 `20260916050532_TicketCustomerCollaboration`，DDL 仅新增 `(TenantId, ReporterUserId)` 索引；snapshot 同步显式属性映射及已有评论 ID 的 ValueGeneratedNever 配置，没有删除列、改写数据或重编号。仅在隔离测试容器应用。
- 部署兼容：旧归属列及唯一索引结构保持兼容；新旧 API 混跑时，旧运营 API 仍看不到客户工单，发布须统一切换应用实例后再验收客服功能。独立租户连接库不在当前单库协作范围，须先做归并方案。
- 未完成：跨身份域附件交换、工单活动通知、SSE/导出及用户搜索联合验收。Files 原租户过滤未放开，不能把正文协作宣称为完整附件协作；现有工单领域事件仍仅记录日志，不宣称已有自动活动通知。因此 4.6 仍不勾选。
- 验证：Integration.Tests 筛选 `FullyQualifiedName~Tests.Tickets|FullyQualifiedName~Tests.Files`，107 项通过、0 跳过；Architecture.Tests 53 项通过，新增工单显式归属及双过滤模型约束。DbMigrator 构建 0 警告/0 错误；TicketsDbContext 的 `has-pending-model-changes` 无差异。固定 dotnet-ef 10.0.2 提示低于运行时 10.0.8，未擅自升级。未执行前端或全方案端到端回归。
- 下一步：为工单附件与通知设计并实现明确的资源/接收者边界，随后完成阶段 4 其余岗位与交付验收，再进入两端页面迁移。
